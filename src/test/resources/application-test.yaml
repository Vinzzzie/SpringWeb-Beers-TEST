spring:
  application:
    name: BierShopApplication
  main:
    banner-mode: off
  datasource:
    url: jdbc:mysql://localhost:3306/beers
    username: bierke
    password: bierke
  test:
    database:
      replace: none
logging:
  level:
    be.vdab.biershop: debug
    root: warn
